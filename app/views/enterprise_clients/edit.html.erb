<div class="container marg2">
    <br>
    <br><br>
        <div class="row">
            <div class="col-lg-12 ">

				<% if user_signed_in? and current_user.tipo == 'administrador' %>

					<h1>Editar Cliente Empresa</h1>

					<%= render 'form' %>

					<%= button_to 'Ver', @enterprise_client, :method => 'get', :class => 'btn btn-info' %> |
					<%= button_to 'Volver', enterprise_clients_path, :method => 'get', :class => 'btn btn-default' %>
				<% end %>

</div></div></div>