<div class="container marg2">
<% if user_signed_in? and current_user.tipo == 'administrador' %>
    <br>
    <br><br>
        <div class="row">
            <div class="col-lg-12 ">
<h1>Editar Conductores</h1>

<%= render 'form' %>

<%= button_to 'Ver', @driver, :method => 'get', :class => 'btn btn-info' %> |
<%= button_to 'Volver', drivers_path, :method => 'get', :class => 'btn btn-default' %>
</div></div>
<% end %>
</div>