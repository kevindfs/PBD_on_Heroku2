<% if user_signed_in? and current_user.tipo == 'administrador' %>
  <div class="container marg2">
    <br>
    <br><br>
        <div class="row">
            <div class="col-lg-12 ">   

	<p id="notice"><%= notice %></p>
	<p>
	  <strong>Lugar de origen:</strong>
	  <%= @fixed_rate.ori_tar %>
	</p>

	<p>
	  <strong>Lugar de destino:</strong>
	  <%= @fixed_rate.dest_tar %>
	</p>

	<p>
	  <strong>Costo servicio:</strong>
	  <%= @fixed_rate.costo_tar %>
	</p>

	<%= button_to 'Editar', edit_fixed_rate_path(@fixed_rate), :method=>'get', :class=>'btn btn-warning' %> |
	<%= button_to 'Volver', fixed_rates_path, :method=>'get', :class=>'btn btn-default' %>

		</div>
	</div>
</div>

<% end %>