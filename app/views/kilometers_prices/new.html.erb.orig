<% if user_signed_in? and current_user.tipo == 'administrador' %>

	<h1>New kilometers_price</h1>

	<%= render 'form' %>

	<%= button_to 'Volver', kilometers_prices_path, :method => 'get', :class => 'btn btn-default' %>


<% end %>